cos

include "stdlib.cat"

let invrf = rf^-1

isla_special_cos

(* From now, co is a coherence order *)
let coe = co & ext
let coi = co \ coe

(* Compute fr *)
let fr = ((invrf ; co) \ id) & loc
let fre = fr & ext
let fri = fr \ fre

show co,fr
